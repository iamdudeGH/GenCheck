# GenLayer SDK examples

Standalone learning experiments written while exploring the GenVM SDK
(hello-world contract, tip jar, ERC-20 with LLM name, prediction market,
etc.). Not part of GenCheck — kept for reference only.

The actual project lives in `contracts/`, `gencheck/`, `demo/`, `scripts/`,
and `tests/`. See the root [README.md](../README.md).
