# Store text as LF in the repo regardless of the Windows checkout, so the
# Dockerfile's line continuations and the shell scripts stay valid on Linux.
* text=auto eol=lf

*.png binary
*.svg text eol=lf
*.csv text eol=lf
